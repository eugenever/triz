<div class="row">
  <div class="col-md-4 col-sm-6 col-xs-12">
    <h1><%= @user.name %></h1>
    <p><%= @user.email %></p>
    <% if current_user.admin? && !current_user?(@user) %>
      <p><%= link_to "Удалить пациента", user, method: :delete, data: { confirm: "Вы уверены?" } %></p>
    <% end %>
  </div>
</div>